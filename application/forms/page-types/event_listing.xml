<?xml version="1.0" encoding="UTF-8"?>
<form i18nBaseUri="page-types.event_listing:">

	<tab id="main">
		<fieldset id="main">
			<!-- add / remove / modify fields in the main tab & fieldset here -->
		</fieldset>
	</tab>

	<tab id="meta">
		<fieldset id="meta">
			<!-- add / remove / modify fields in the main tab & fieldset here -->
		</fieldset>
	</tab>

	<tab id="dates">
		<fieldset id="dates">
			<!-- add / remove / modify fields in the main tab & fieldset here -->
		</fieldset>
	</tab>

	<tab id="event_listing">
		<fieldset id="event_listing">
			<field binding="event_listing.event_detail" />
			<field binding="event_listing.region" quickAdd="true" quickEdit="true" sortable="true" />
			<field binding="event_listing.categories" quickAdd="true" quickEdit="true" sortable="true" />
		</fieldset>
	</tab>
</form>